{
  "version": 2,
  "builds": [
    {
      "src": "src/App.js",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "build",
        "distDir": "src"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/public/index.html"
    }
  ]
}
